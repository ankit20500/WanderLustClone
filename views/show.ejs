<% layout("/layouts/boilerPlate") -%>

    <div class="row">
    <div class="card col-8 offset-2"><br>
        <h3>Listing Details:</h3>
        <img src="<%= lists.image.url %>" class="card-show-img-top" alt="lists_image">
        <div class="card-body">
          <p class="card-text">
            <b><%= lists.title %></b><br>
            <%= lists.description %> <br>
            &#8377; <%= lists.price.toLocaleString("en-IN") %> <br>
            <%= lists.location %> <br>
            <%= lists.country %> <br>
        </p>
        </div>
      

    <!-- <ul>
        <li><%= lists.title %></li>
        <li><%= lists.description %></li>
        <li><%= lists.image %></li>
        <li>&#8377; <%= lists.price.toLocaleString("en-IN") %></li>
        <li><%= lists.location %></li>
        <li><%= lists.country %></li>
    </ul> -->
   
    <a href=" http://localhost:5050/listing/edit/<%= lists._id %>">Edit</a>
    <br><br>
    <form method="POST" action="/listing/<%= lists._id %>?_method=DELETE">
        <button type="delete" class="btn btn-dark add-btn">Delete</button></form>
</div>
</div>